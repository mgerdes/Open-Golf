struct foobar {
    int x, y, sum;
}

void do_stuff(foobar* f) {
    *f.sum = *f.x + *f.y;
}

int run(int n0, int n1, void* file_ptr) {
    foobar f = { x = n0, y = n1, sum = 0 };
    do_stuff(&f);
    print("f = ", f, "\n");
    return 100;
}
